<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulaire de Suppression</title>
    <style>
        /* Styling for the Delete form container */
        .delete-form {
            display: inline-block;
            margin: 10px;
            text-align: center;
        }

        /* Styling for the Delete button */
        .btn-delete {
            background-color: #e74c3c;  /* Red color to signify danger */
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        /* Hover effect for the Delete button */
        .btn-delete:hover {
            background-color: #c0392b;  /* Darker red */
            transform: scale(1.05);  /* Slight scaling effect for emphasis */
        }

        /* Confirmation prompt styling */
        .delete-form button {
            text-decoration: none;
            font-weight: bold;
        }

        .delete-form button:focus {
            outline: none; /* Remove outline when button is clicked */
        }

        /* Optional: Add a shadow to make it stand out more */
        .btn-delete {
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        }

        .btn-delete:active {
            background-color: #b03a2e; /* Even darker red on click */
        }
    </style>
</head>
<body>

    <div class="delete-form">
        <form method="post" action="{{ path('app_panier_delete', {'id': panier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panier.id) }}">
            <button type="submit" class="btn-delete">Delete</button>
        </form>
    </div>

</body>
</html>
