{% extends 'admin/base.html.twig' %}

{% block title %}Event Details{% endblock %}

{% block content %}
<div class="container">
    <h2 class="my-4">Event Details</h2>

    <div class="card p-4">
        <table class="table">
            <tbody>
                <tr>
                    <th>Nom</th>
                    <td>{{ event.nom }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ event.description }}</td>
                </tr>
                <tr>
                    <th>Heure début</th>
                    <td>{{ event.heurDebut ? event.heurDebut|date('Y-m-d H:i:s') : 'N/A' }}</td>
                </tr>
                <tr>
                    <th>Heure fin</th>
                    <td>{{ event.heurFin ? event.heurFin|date('Y-m-d H:i:s') : 'N/A' }}</td>
                </tr>
               <th>Image</th>
            </tbody>
        </table>
        <tr>
                    
                    <div class="image-container">
                            {% if event.image %}
                                 <img src="/uploads/images/{{ event.image }}" alt="Image de l'Événement">
                             {% else %}
                               <p>Aucune image disponible</p>
                             {% endif %}
                     </div>
                </tr>
    </div>

    <div class="mt-3">
        <a href="{{ path('app_event_index_back') }}" class="btn btn-secondary">⬅ Retour</a>
        <a href="{{ path('app_event_edit', {'id': event.id}) }}" class="btn btn-primary">Modifier</a>
        <a href="{{ path('event_pdf', {'id': event.id}) }}" class="btn btn-info">📄 Télécharger PDF</a>
    </div>

    <div class="mt-3">
        {{ include('admin/event/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
