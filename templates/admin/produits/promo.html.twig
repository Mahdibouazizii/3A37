{% extends 'admin/base.html.twig' %}

{% block title %}Modifier la Promotion{% endblock %}

{% block content %}
<div class="container mt-4">
    <h2 class="text-center">Mettre en Promotion: {{ produit.nom }}</h2>

    <div class="card p-4">
        {{ form_start(form) }}
            <div class="mb-3">
                {{ form_label(form.promoPercentage) }}
                {{ form_widget(form.promoPercentage) }}
            </div>
            <button type="submit" class="btn btn-primary w-100">Mettre à jour</button>
        {{ form_end(form) }}
    </div>

    <div class="text-center mt-3">
        <a href="{{ path('admin_produit_index') }}" class="btn btn-secondary">Retour</a>
    </div>
</div>
{% endblock %}
