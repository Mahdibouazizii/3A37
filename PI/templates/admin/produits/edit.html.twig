{% extends 'admin/base.html.twig' %}

{% block title %}Modifier la Promotion{% endblock %}

{% block content %}
    <h2 class="mb-4">Modifier la Promotion</h2>

    <div class="card p-4">
        {{ form_start(form) }}
            <div class="mb-3">
                {{ form_label(form.promoPercentage, 'Pourcentage de réduction (%)') }}
                {{ form_widget(form.promoPercentage, {'attr': {'class': 'form-control'}}) }}
            </div>
            <button type="submit" class="btn btn-primary">Enregistrer</button>
        {{ form_end(form) }}
    </div>

    <a href="{{ path('admin_produit_index') }}" class="btn btn-secondary mt-3">Retour</a>
{% endblock %}
